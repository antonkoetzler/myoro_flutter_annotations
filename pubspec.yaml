name: myoro_flutter_annotations
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  build_runner: ^2.4.15
  source_gen: ^2.0.0
  analyzer: ^7.4.5
  build: ^2.4.2
  dart_style: ^3.1.0
  pub_semver: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
