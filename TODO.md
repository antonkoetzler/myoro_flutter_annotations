# TODO

1. refactor: Remove `Element`/`ClassElement` usage when `source_gen` fixes the deprecation;
2. Make a repo for common Bash scripts in Dart

- Formatter script
- Coverage script (for Flutter and Dart, check `README.md`)

3. CI/CD;
4. Publish to <https://pub.dev>.
